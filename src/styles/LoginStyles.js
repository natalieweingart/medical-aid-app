import {StyleSheet} from 'react-native';

const loginStyles = StyleSheet.create ({
    txtTitle: {
      fontSize: 40,
      textAlign: 'center',
      color: 'black',
      fontWeight: 'bold',
    },
    btnTxt: {
      fontSize: 18,
      fontWeight: 'bold',
      color: 'black',
      alignSelf: 'center',
    }
})

export default loginStyles